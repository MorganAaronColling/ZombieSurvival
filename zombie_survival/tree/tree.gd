extends StaticBody2D


# Called when the node enters the scene tree for the first time.
#func _ready():
#	var tree_sprite = $TreeSprite
#	var tree_collision = $TreeCollision
#	var scale_factor = randf_range(0.8, 1.2)
#	tree_sprite.scale *= Vector2(scale_factor, scale_factor)
#	tree_collision.scale *= Vector2(scale_factor, scale_factor)
#	if randf_range(0, 1) > 0.5:
#		tree_sprite.flip_h = true
		
		


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
